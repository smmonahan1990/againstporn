import { BrowserRouter } from 'react-router-dom';
import BaseLayout from './BaseLayout';

const Router = () => {
  return (
    <BrowserRouter>
      <BaseLayout />
    </BrowserRouter>
  )
}

export default Router;
